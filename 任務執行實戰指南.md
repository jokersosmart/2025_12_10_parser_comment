# 任務執行實戰指南

**建立日期**: 2025-11-19  
**任務清單**: [../specs/001-social-comment-scraper/tasks.md](../specs/001-social-comment-scraper/tasks.md)  
**目的**: 協助您實際執行 116 個任務，從零到完成 MVP

---

## 📋 任務清單總覽

**已生成的任務清單**: `specs/001-social-comment-scraper/tasks.md`

**統計**:
- **總任務數**: 116 個
- **MVP 任務數**: 34 個（Phase 1-3）
- **預估總時程**: 14.5 天（單人）
- **MVP 時程**: 6.5 天
- **可並行任務**: ~65%

---

## 🎯 執行策略選擇

### 策略 A: MVP 優先（推薦）⭐

**目標**: 最快看到可運作的產品

**執行順序**:
```
Phase 1: Setup (T001-T010) → 0.5 天
Phase 2: Foundation (T011-T035) → 2 天
Phase 3: US1 - 爬取功能 (T036-T053) → 3 天
---
MVP 完成！可展示和測試
```

**何時使用**: 
- 想要快速驗證概念
- 需要儘早展示給使用者
- 單人開發
- 時間緊迫

---

### 策略 B: 完整開發

**目標**: 一次完成所有功能

**執行順序**:
```
Phase 1-7 依序執行
---
完整功能！
```

**何時使用**:
- 有充足時間（3 週）
- 團隊有多位開發者
- 想要一次交付完整產品

---

### 策略 C: 並行開發（多人團隊）

**目標**: 加速開發

**分工**:
```
開發者 A: Phase 1-2（大家一起）→ US1 爬取功能
開發者 B: Phase 1-2（大家一起）→ US2 編輯功能
開發者 C: 後端 API 代理 + 監控設置
```

**何時使用**:
- 有 2-3 位開發者
- 想要縮短時程到 7-10 天

---

## 📅 第一週執行計劃（MVP 路徑）

### Day 1: 環境準備 + Setup

**上午（4 小時）**:
- [ ] 申請 Meta API（2-3 小時）
  - 按照「Meta-API-申請完整指南.md」執行
  - 提交權限審核
- [ ] 休息 ☕

**下午（4 小時）**:
- [ ] T001-T010: Setup 任務（全部可並行）
  - T001: 建立目錄結構 ✅ 已完成
  - T002: 初始化 frontend 專案
  - T003: 建立 api/ 結構
  - T004: 設定 ESLint/Prettier
  - T005: 設定 Tailwind CSS
  - T006-T010: 其他設定

**產出**: 專案骨架完成

---

### Day 2: Foundation Part 1（後端 API 代理）

**重要**: 如果 API 還在審核，使用 Mock API 開發

**上午（4 小時）**:
- [ ] T011-T013: 後端核心模組（可並行）
  - T011: Token 管理（先用假 Token）
  - T012: 速率限制（Token Bucket）
  - T013: 錯誤映射（繁中）

**下午（4 小時）**:
- [ ] T014-T017: API 代理端點
  - T014: Instagram 代理（依賴 T011-T013）
  - T015: Facebook 代理（依賴 T011-T013）
  - T016: Health 端點
  - T017: 後端測試

**產出**: 後端 API 代理層完成（可用 Mock 測試）

---

### Day 3: Foundation Part 2（IndexedDB + 基礎元件）

**上午（4 小時）**:
- [ ] T018-T024: IndexedDB 資料層（部分可並行）
  - T018: Dexie Schema 設定
  - T019-T023: 資料存取層（可並行）
  - T024: 儲存層測試

**下午（4 小時）**:
- [ ] T025-T029: 核心服務
  - T025: URL 解析
  - T026: 後端客戶端
  - T027: 時間格式化
  - T028-T029: 錯誤處理 + 測試

**產出**: 資料層和核心服務完成

---

### Day 4: Foundation Part 3 + US1 開始

**上午（4 小時）**:
- [ ] T030-T035: 基礎 UI 元件（全部可並行）
  - T030: Button
  - T031: Input
  - T032: Modal
  - T033: Toast
  - T034: ProgressBar
  - T035: 元件測試

**下午（4 小時）**:
- [ ] T036-T038: US1 測試（TDD，必須先寫）
  - T036: URL 解析測試
  - T037: 爬蟲契約測試（MSW）
  - T038: 爬取整合測試

**產出**: Foundation 完成！可以開始 US1

---

### Day 5: US1 核心實作

**上午（4 小時）**:
- [ ] T039-T043: 爬蟲核心邏輯
  - T039: 爬蟲服務核心（使用 Mock API）
  - T040-T041: 資料正規化（可並行）
  - T042: 自動續傳邏輯
  - T043: useScraper Hook

**下午（4 小時）**:
- [ ] T044-T047: US1 UI 實作
  - T044-T045: 網址輸入 + 進度（可並行）
  - T046: CommentsTable 基礎版
  - T047: Home 頁面整合

**產出**: 爬取功能可視化

---

### Day 6: US1 錯誤處理 + 整合

**上午（4 小時）**:
- [ ] T048-T050: 錯誤處理（全部可並行）
  - T048: 網址驗證
  - T049: 網路錯誤重試
  - T050: 錯誤訊息（繁中）

**下午（4 小時）**:
- [ ] T051-T053: 整合與驗證
  - T051: 整合所有功能到 Home
  - T052: E2E 測試
  - T053: 效能測試

**產出**: US1 完全完成 ✅

---

### Day 7: MVP 驗證與修復

**全天（8 小時）**:
- [ ] 完整測試 MVP
- [ ] 修復發現的 bugs
- [ ] 優化使用者體驗
- [ ] 準備展示或部署

**如果 API 審核通過**:
- [ ] 整合真實 API Token
- [ ] 測試真實爬取
- [ ] 部署到 Vercel

**產出**: **MVP 完成！** 🎉

---

## 📝 任務執行範本

### 每個任務的標準流程

```
1. 開啟 tasks.md 找到任務
   例如：T002 初始化前端專案
   
2. 閱讀任務描述和檔案路徑
   frontend/ - React + TypeScript + Vite
   
3. 查看相關文件（如需要）
   - plan.md: 技術決策
   - research.md: 為什麼選這個技術
   - quickstart.md: 具體操作步驟
   
4. 執行任務
   pnpm create vite frontend --template react-ts
   
5. 驗證完成
   - 專案可啟動
   - 無錯誤
   
6. 勾選任務
   - [ ] T002 → - [x] T002
   
7. Commit
   git commit -m "feat: 完成 T002 - 初始化前端專案"
```

---

## 🔄 實際操作範例

### 範例 1: T002 - 初始化前端專案

**任務內容**:
```
- [ ] T002 [P] 初始化前端 React + TypeScript + Vite 專案在 frontend/
```

**執行步驟**:

```powershell
# 1. 進入專案目錄
cd C:\Users\user\Desktop\Cursor\social-comment-scraper

# 2. 使用 Vite 建立專案
pnpm create vite frontend --template react-ts
# 選擇：React, TypeScript

# 3. 進入 frontend 目錄
cd frontend

# 4. 安裝依賴
pnpm install

# 5. 測試執行
pnpm dev
# 應開啟 http://localhost:5173

# 6. 驗證
# ✅ 看到 Vite + React 預設頁面
# ✅ 無 Console 錯誤

# 7. 停止開發伺服器（Ctrl+C）

# 8. Commit
cd ..
git add frontend/
git commit -m "feat: 完成 T002 - 初始化前端 React+TS 專案"
```

**完成時間**: 5-10 分鐘

**勾選**: tasks.md 中將 `- [ ] T002` 改為 `- [x] T002`

---

### 範例 2: T005 - 設定 Tailwind CSS

**任務內容**:
```
- [ ] T005 [P] 設定 Tailwind CSS 在 frontend/
```

**執行步驟**:

```powershell
# 1. 進入 frontend
cd frontend

# 2. 安裝 Tailwind
pnpm add -D tailwindcss postcss autoprefixer

# 3. 初始化配置
pnpm dlx tailwindcss init -p

# 4. 編輯 tailwind.config.js
# 設定 content 路徑：
content: [
  "./index.html",
  "./src/**/*.{js,ts,jsx,tsx}",
]

# 5. 編輯 src/index.css
# 加入 Tailwind directives：
@tailwind base;
@tailwind components;
@tailwind utilities;

# 6. 測試
# 在 App.tsx 中加入 Tailwind 類別測試
<div className="text-blue-500 font-bold">測試 Tailwind</div>

# 7. 啟動看效果
pnpm dev

# 8. Commit
git add .
git commit -m "feat: 完成 T005 - 設定 Tailwind CSS"
```

**完成時間**: 10-15 分鐘

---

### 範例 3: T018 - 設定 Dexie.js Schema

**任務內容**:
```
- [ ] T018 設定 Dexie.js 資料庫 Schema frontend/src/services/storage/db.ts
```

**執行步驟**:

```powershell
# 1. 安裝 Dexie
cd frontend
pnpm add dexie

# 2. 建立目錄結構
mkdir -p src/services/storage

# 3. 建立 db.ts 檔案
# 參考 data-model.md 中的 Schema 定義
```

**檔案內容** (`frontend/src/services/storage/db.ts`):
```typescript
import Dexie, { Table } from 'dexie';

// 匯入類型定義
import type { Post, Comment, ScrapingState, CustomField, OperationHistory, StorageQuotaStatus } from '../../types';

export class ScraperDatabase extends Dexie {
  posts!: Table<Post>;
  comments!: Table<Comment>;
  scrapingStates!: Table<ScrapingState>;
  customFields!: Table<CustomField>;
  operationHistory!: Table<OperationHistory>;
  storageQuota!: Table<StorageQuotaStatus>;

  constructor() {
    super('SocialCommentScraperDB');
    
    this.version(1).stores({
      posts: '++id, url, platform, scrapedAt, [platform+createdAt]',
      comments: '++id, postId, commentId, timestamp, [postId+timestamp]',
      scrapingStates: '++id, postUrl, status, startedAt',
      customFields: '++id, postId, fieldName',
      operationHistory: '++id, timestamp, type',
      storageQuota: '++id, lastChecked'
    });
  }
}

export const db = new ScraperDatabase();
```

```powershell
# 4. 測試
# 在 App.tsx 中測試能否連線
import { db } from './services/storage/db';

// 測試寫入
await db.posts.add({ 
  url: 'test', 
  platform: 'instagram',
  scrapedAt: new Date(),
  totalComments: 0
});

# 5. Commit
git add src/services/storage/db.ts
git commit -m "feat: 完成 T018 - 設定 Dexie.js Schema"
```

**完成時間**: 20-30 分鐘

---

## 🚀 快速開始：執行前 10 個任務

### Phase 1 完整執行（T001-T010）

**這 10 個任務都可以並行！**

**執行腳本**（一次完成所有 Setup）:

```powershell
# 進入專案目錄
cd C:\Users\user\Desktop\Cursor\social-comment-scraper

# T001: 已完成 ✅

# T002: 初始化 frontend
pnpm create vite frontend --template react-ts
cd frontend
pnpm install
cd ..

# T003: 建立 api 結構
mkdir api\_lib api\_tests

# T004: ESLint 設定
cd frontend
pnpm add -D eslint @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint-config-prettier

# 建立 .eslintrc.json
@"
{
  \"extends\": [
    \"eslint:recommended\",
    \"plugin:@typescript-eslint/recommended\",
    \"plugin:react-hooks/recommended\",
    \"prettier\"
  ],
  \"rules\": {
    \"complexity\": [\"error\", 10]
  }
}
"@ | Out-File -FilePath .eslintrc.json -Encoding UTF8

# T005: Tailwind CSS
pnpm add -D tailwindcss postcss autoprefixer
pnpm dlx tailwindcss init -p

# T006: TypeScript 類型目錄
mkdir src\types

# T007: 環境變數範本
cd ..
@"
INSTAGRAM_APP_TOKEN=
FACEBOOK_APP_TOKEN=
RATE_LIMIT_MAX_CALLS=180
"@ | Out-File -FilePath .env.example -Encoding UTF8

# T008: vercel.json
@"
{
  \"version\": 2,
  \"builds\": [
    { \"src\": \"frontend/package.json\", \"use\": \"@vercel/static-build\" },
    { \"src\": \"api/**/*.ts\", \"use\": \"@vercel/node\" }
  ]
}
"@ | Out-File -FilePath vercel.json -Encoding UTF8

# T009: Git hooks
cd frontend
pnpm add -D husky lint-staged
pnpm dlx husky init

# T010: README
cd ..
@"
# 社群留言爬蟲工具

線上工具，爬取 Instagram 和 Facebook 公開貼文留言。

## 快速開始

詳見：開發環境準備指南.md
"@ | Out-File -FilePath README.md -Encoding UTF8

# 完成！
Write-Host "`n✅ Phase 1 Setup 完成！" -ForegroundColor Green
```

**完成時間**: 30-60 分鐘（一次執行所有）

**Commit**:
```powershell
git add .
git commit -m "feat: 完成 Phase 1 Setup (T001-T010)

- 初始化 frontend（React + TS + Vite）
- 建立 api 目錄結構
- 設定 ESLint 和 Prettier
- 設定 Tailwind CSS
- 建立 TypeScript 類型目錄
- 設定環境變數範本
- 設定 Vercel 部署
- 設定 Git hooks
- 建立 README"
```

---

## 🧪 TDD 實作範例（測試驅動開發）

### 什麼是 TDD？

**TDD = Test-Driven Development（測試驅動開發）**

**流程**:
```
1. 寫測試（會失敗）
2. 寫最少的程式碼讓測試通過
3. 重構優化
4. 重複
```

### 實際範例：T036-T039

#### Step 1: 寫測試（T036）

```typescript
// frontend/tests/unit/url-parser.test.ts
import { describe, it, expect } from 'vitest';
import { parseInstagramUrl } from '../../src/services/api/url-parser';

describe('parseInstagramUrl', () => {
  it('應正確解析標準 Instagram 網址', () => {
    const result = parseInstagramUrl('https://www.instagram.com/p/ABC123/');
    expect(result).toBe('ABC123');
  });
  
  it('應拒絕無效網址', () => {
    const result = parseInstagramUrl('https://invalid.com');
    expect(result).toBeNull();
  });
});
```

**執行測試**:
```powershell
pnpm test
# ❌ 測試失敗！（因為還沒寫 url-parser.ts）
```

---

#### Step 2: 寫最少程式碼讓測試通過（T025）

```typescript
// frontend/src/services/api/url-parser.ts
export function parseInstagramUrl(url: string): string | null {
  const regex = /instagram\.com\/p\/([A-Za-z0-9_-]+)/;
  const match = url.match(regex);
  return match ? match[1] : null;
}
```

**執行測試**:
```powershell
pnpm test
# ✅ 測試通過！
```

---

#### Step 3: 繼續下一個功能（T039）

現在 URL 解析有了，可以開始寫爬蟲服務。

**先寫測試（T037）**:
```typescript
// frontend/tests/unit/scraper-service.test.ts
test('應成功爬取留言', async () => {
  // 使用 MSW Mock API
  const comments = await scrapeComments('https://instagram.com/p/TEST/');
  expect(comments.length).toBeGreaterThan(0);
});
```

**再寫實作（T039）**:
```typescript
// frontend/src/services/api/scraper-service.ts
export async function scrapeComments(postUrl: string) {
  const mediaId = parseInstagramUrl(postUrl);
  const response = await axios.post('/api/instagram/scrape', { postUrl });
  return response.data.comments;
}
```

**這就是 TDD！** 先測試後實作，確保程式碼可測試。

---

## 🎯 任務追蹤技巧

### 技巧 1: 視覺化進度

**建立進度追蹤檔案**:

```markdown
# 我的開發進度.md

## Week 1: MVP

### Phase 1: Setup ✅
- [x] T001-T010 全部完成

### Phase 2: Foundation 🔄
- [x] T011-T017 後端完成
- [x] T018-T024 IndexedDB 完成
- [ ] T025-T035 進行中...

### Phase 3: US1 ⏳
- [ ] 待開始

---

當前任務：T028 錯誤處理工具
預計完成：今天下午
```

---

### 技巧 2: 使用 GitHub Projects

**建立 Project Board**:
```
1. 在 GitHub repository
2. Projects → New project
3. Board view
4. 欄位：
   - 📋 Todo
   - 🔄 In Progress
   - ✅ Done

5. 將 tasks.md 的任務建立為 Issues
6. 拖曳管理進度
```

---

### 技巧 3: 每日站立會議（個人版）

**每天開始前（5 分鐘）**:
```
昨天完成：
- T011-T013 後端核心模組
- T014 Instagram 代理端點

今天計劃：
- T015 Facebook 代理端點
- T016-T017 Health + 測試

遇到的問題：
- Vercel Functions 本地測試需要安裝 CLI
```

---

### 技巧 4: 批次 Commit

**相關任務一起 Commit**:

```powershell
# 完成 T011, T012, T013（都是後端核心）
git add api/_lib/
git commit -m "feat: 完成 T011-T013 後端核心模組

- T011: Token 管理
- T012: 速率限制（Token Bucket）
- T013: 錯誤映射（繁中）"
```

**vs 每個任務單獨 Commit**:
```powershell
# 也可以，但會有很多小 commits
git commit -m "feat: T011 Token 管理"
git commit -m "feat: T012 速率限制"
git commit -m "feat: T013 錯誤映射"
```

**建議**: 相關任務批次 Commit（2-5 個一起）

---

## 🐛 遇到問題時

### 問題解決流程

```
1. 檢查錯誤訊息
   - 仔細閱讀 error message
   - 注意行號和檔案名稱
   
2. 查看相關文件
   - research.md: 技術決策理由
   - data-model.md: 資料結構
   - quickstart.md: 除錯指南
   
3. 檢查範例程式碼
   - plan.md 中有範例
   - research.md 中有實作細節
   
4. Google 或 AI 協助
   - 搜尋錯誤訊息
   - 詢問 Cursor AI
   
5. 記錄解決方案
   - 更新 History.md
   - 或建立 troubleshooting.md
```

---

### 常見問題快速解法

#### 問題 1: pnpm 指令不存在

```powershell
# 解決
npm install -g pnpm
```

#### 問題 2: Vite 建立專案失敗

```powershell
# 解決：使用 npm
npm create vite@latest frontend -- --template react-ts
```

#### 問題 3: TypeScript 型別錯誤

```powershell
# 解決：先定義類型
# 參考 data-model.md 建立 types/
```

#### 問題 4: Git commit 出現亂碼

```powershell
# 解決：執行編碼修復
..\fix-encoding.ps1
# 重新啟動 PowerShell
```

---

## 📊 進度追蹤儀表板

### 建議的追蹤方式

**每天更新**:

```markdown
# 開發進度儀表板

## 總覽
- 總任務：116
- 已完成：15 ✅
- 進行中：3 🔄
- 待開始：98 ⏳
- 完成度：13%

## 本週目標
- [x] Phase 1: Setup
- [x] Phase 2: Foundation（50%）
- [ ] Phase 3: US1（目標：週五完成）

## 當前障礙
- ⏳ 等待 Meta API 審核（第 3 天）
- ✅ 使用 Mock API 繼續開發

## 下週計劃
- 整合真實 API
- US2 編輯功能
```

---

## 🎯 關鍵里程碑

### Milestone 1: Setup 完成

**任務**: T001-T010  
**時間**: 0.5 天  
**檢查**:
- [ ] 專案目錄結構正確
- [ ] frontend 可啟動（pnpm dev）
- [ ] ESLint 無錯誤
- [ ] Tailwind 可使用
- [ ] Git 正常運作

---

### Milestone 2: Foundation 完成 ⭐

**任務**: T011-T035  
**時間**: 2 天  
**檢查**:
- [ ] 後端 API 代理可執行
- [ ] IndexedDB 可讀寫
- [ ] 基礎 UI 元件可用
- [ ] 測試可執行（pnpm test）
- [ ] 所有測試通過

**重要**: 此 Milestone 完成前，不能開始任何 User Story！

---

### Milestone 3: MVP 完成 🎉

**任務**: T036-T053  
**時間**: 3 天  
**檢查**:
- [ ] 可輸入網址並爬取（Mock API）
- [ ] 留言顯示在表格中
- [ ] 進度指示器正常
- [ ] 錯誤訊息正確顯示（繁中）
- [ ] E2E 測試通過
- [ ] 效能符合目標（<5 秒）

**成就解鎖**: 可展示的 MVP！

---

## 💡 效率提升技巧

### 技巧 1: 善用 [P] 標記

**[P] = 可並行**

如果您有多個終端視窗或使用 Tmux：

```powershell
# 終端 1
cd frontend/src/components/common
# 實作 T030: Button.tsx

# 終端 2  
cd frontend/src/components/common
# 實作 T031: Input.tsx

# 終端 3
cd frontend/src/components/common
# 實作 T032: Modal.tsx

# 同時進行！
```

---

### 技巧 2: 使用程式碼產生器

**Cursor AI 協助**:
```
"根據 data-model.md 的 Post interface，
幫我生成 TypeScript 類型定義檔案"

# AI 會自動產生 types/post.ts
```

---

### 技巧 3: 批次操作

**一次建立多個檔案**:

```powershell
# 建立所有 UI 元件檔案
$components = @('Button', 'Input', 'Modal', 'Toast', 'ProgressBar')

foreach ($comp in $components) {
  New-Item -Path "frontend/src/components/common/$comp.tsx" -ItemType File
  
  # 加入基本範本
  @"
import React from 'react';

interface ${comp}Props {
  // TODO: 定義 props
}

export const ${comp}: React.FC<${comp}Props> = (props) => {
  return <div>${comp}</div>;
};
"@ | Out-File -FilePath "frontend/src/components/common/$comp.tsx" -Encoding UTF8
}

Write-Host "✅ 5 個元件檔案已建立"
```

---

## 📋 每日工作流程建議

### 早上（9:00 - 12:00）

```
09:00 - 09:15  查看 tasks.md，選擇今天的任務
09:15 - 10:00  執行第一個任務
10:00 - 10:15  休息 ☕
10:15 - 11:30  繼續執行任務
11:30 - 12:00  測試和 Commit
```

### 下午（13:00 - 18:00）

```
13:00 - 14:30  執行任務
14:30 - 14:45  休息
14:45 - 16:30  執行任務  
16:30 - 17:00  測試和優化
17:00 - 17:30  Commit 和更新進度
17:30 - 18:00  規劃明天任務
```

**每天產出**: 5-8 個任務完成

---

## ✅ 完整 Checklist（列印使用）

```
□ Day 1: 申請 Meta API + Setup（T001-T010）
□ Day 2: Foundation Part 1（T011-T024）
□ Day 3: Foundation Part 2（T025-T035）
□ Day 4: US1 測試 + 核心（T036-T043）
□ Day 5: US1 UI + 錯誤（T044-T050）
□ Day 6: US1 整合（T051-T053）
□ Day 7: MVP 驗證和部署

✅ MVP 完成！
```

---

## 🎊 完成 MVP 時的檢查清單

**功能檢查**:
- [ ] 可輸入 Instagram 網址並爬取留言（使用 Mock API）
- [ ] 可輸入 Facebook 網址並爬取留言（使用 Mock API）
- [ ] 留言顯示在表格中（包含所有欄位）
- [ ] 時間顯示為本地時間（GMT+8）
- [ ] 進度指示器正確更新
- [ ] 錯誤訊息用繁體中文顯示
- [ ] 網路中斷時保存資料（自動續傳）

**品質檢查**:
- [ ] 測試覆蓋率 ≥80%
- [ ] ESLint 零警告
- [ ] 所有測試通過
- [ ] 頁面載入 <2 秒
- [ ] 無 Console 錯誤

**部署檢查**:
- [ ] 可在本地執行（localhost:5173）
- [ ] 可部署到 Vercel
- [ ] 環境變數正確設定

---

**指南版本**: 1.0  
**最後更新**: 2025-11-19

🚀 **準備好開始執行任務了！從 T002 開始吧！**

